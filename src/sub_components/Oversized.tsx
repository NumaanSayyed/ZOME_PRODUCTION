import Collection from './Collection';
import Oversize from '../JSON/Oversize.json';

function Oversized() {

  return (
    <>
      <Collection products={Oversize} />
    </>
  );
}

export default Oversized;
